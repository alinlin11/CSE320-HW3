#include <stdio.h>
#include "sfmm.h"

int main(int argc, char const *argv[]) {
    // double* ptr = sf_malloc(sizeof(double));

    // *ptr = 320320320e-320;

    // printf("%f\n", *ptr);

    // sf_free(ptr);

    // sf_show_heap();

//    size_t sz_u = 200, sz_v = 200, sz_w = 200, sz_x = 500, sz_y = 200, sz_z = 700;
//     void *u = sf_malloc(sz_u);
//     void *v = sf_malloc(sz_v);
//     void *w = sf_malloc(sz_w);
//     sf_malloc(sz_x);
//     sf_malloc(sz_y);
//     sf_malloc(sz_z);

//     sf_free(u);
//     sf_free(w);
//     sf_free(v);
//     sf_show_heap();


    return EXIT_SUCCESS;
}
